# Force npm to skip platform-specific checks during install
force=true
# Install all optional dependencies
optional=true
# Ignore platform-specific incompatibilities
--no-engine-strict=true
# Use pnpm for installs
use-pnpm=true
# Don't use frozen lockfile
frozen-lockfile=false
# Always create a new lockfile if needed
save-exact=true 