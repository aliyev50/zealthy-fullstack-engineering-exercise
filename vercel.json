{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "next build",
        "installCommand": "pnpm install --no-frozen-lockfile --no-recursive"
      }
    }
  ]
} 